working_dir: "/tmp/postgres-ci/builds/"
assets: "/opt/postgres-ci/worker/assets"
num_workers: auto # number or auto (defines the number of parallel build process)

debug:
  host: 10.211.55.6
  port: 8080

logger:
  level: "info" # info/warning/error
  logfile: "/var/log/postgres-ci/worker.log"

connect:
  host: "/var/run/postgresql"
  #host: 172.16.9.131
  port: 5432
  username: "postgres_ci"
  password: "iAmAdiscoDancer"
  database: "postgres_ci"

docker:
  endpoint: "unix:///var/run/docker.sock"
  tls_cert_path:
  #binds:
  #  - "/usr/bin/wget:/usr/bin/wget"
  auth:
    username:
    password:
    email:
    server_address:
